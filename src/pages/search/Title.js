import React from 'react';

const Title = ({title}) => {
    return (
        <h2 className='text-gray-600 mb-2 font-semibold'>
            {title}
        </h2>
    );
};

export default Title;